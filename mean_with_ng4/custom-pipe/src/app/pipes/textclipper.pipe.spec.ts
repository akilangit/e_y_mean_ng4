import { TextclipperPipe } from './textclipper.pipe';

describe('TextclipperPipe', () => {
  it('create an instance', () => {
    const pipe = new TextclipperPipe();
    expect(pipe).toBeTruthy();
  });
});
